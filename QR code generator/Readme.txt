We also have to include two jar files to use Googles Zxing library.
If you are familiar with maven then we can add dependencies in our pom.xml file. Else add the two jar files named 

core-3.3.0.jar and
javase-3.3.0.jar

also i have added it here


In this QR code generator we can enter anything in the form of string either name,location,price,information and get its QR code generated.

The advantage of this QR code is that the actual information remains hidden from easily read and changed.